/** Height of the mobile bottom sheet in peek state (px). Keep in sync with Panel's peek height and the CSS --sidebar-width variable. */
export const PANEL_PEEK_HEIGHT_PX = 134;

/** Width of the desktop right sidebar (px). Keep in sync with the sidebar column in page.tsx and the CSS --sidebar-width variable in globals.css. */
export const DESKTOP_SIDEBAR_WIDTH_PX = 380;
